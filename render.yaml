services:
  - type: web
    name: rental-manager
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: PORT
        value: 10000
      - key: RENTAL_MANAGER_DATABASE_URL
        sync: false

