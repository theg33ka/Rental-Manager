meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  persistenceMount: /data
  image: null
  command: sh
  args: scripts/start-container.sh
  containerPort: 10000
  servicePort: 80
