meta:
  environment: docker
  toolchain: docker

build:
  dockerfile: Dockerfile
  skip: false

run:
  command: uvicorn
  args: rental_manager.main:app --host 0.0.0.0 --port 10000
  containerPort: 10000
